# Протокол моста Python ↔ Godot

Статус: Этап 1 реализован и проверен. Дополнять по мере реализации этапов 2-3.
Это единственный источник правды по формату сообщений — если меняешь
формат в коде, обнови и этот файл в том же коммите.

## Транспорт

WebSocket. Python — сервер, Godot — клиент.
Python запускается первым и занимает порт; Godot переподключается каждые 2 с.

Адрес: `ws://localhost:8765` (BRIDGE_HOST/BRIDGE_PORT в .env)

## Направление: Python → Godot

Все сообщения — JSON-объект с полем `type`.

### speech_state (Этап 1)

Сигнализирует, говорит ли сейчас Ника.

```json
{"type": "speech_state", "state": "talking"}
```
```json
{"type": "speech_state", "state": "idle"}
```

### visemes (Этап 2)

Временная шкала визём для покадровой анимации рта, синхронизированная
с проигрыванием WAV-файла на стороне Python.

```json
{
  "type": "visemes",
  "data": [
    {"time": 0.0,  "shape": "X"},
    {"time": 0.12, "shape": "B"},
    {"time": 0.24, "shape": "C"}
  ]
}
```

`shape` — буквенный код виземы Rhubarb (A-H, X). Соответствие
код → форма рта/blend shape фиксируется в `character_controller.gd`.

### action (Этап 3)

```json
{"type": "action", "name": "wave"}
```

`name` — должно соответствовать имени анимации, зарегистрированной
в Animator на стороне Godot. Список доступных имён поддерживать
в этом файле по мере добавления:

- (пока пусто, заполнится на Этапе 3)

## Направление: Godot → Python

На Этапе 1 не требуется. Зарезервировано на будущее (например,
подтверждение, что анимация действия завершилась, или статус готовности
сцены).

## Версионирование

Если формат сообщения меняется несовместимо — добавить поле `"v": 2`
и обработать обе версии на переходный период, либо синхронно обновить
обе стороны в одном PR.
