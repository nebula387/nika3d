# Скопировать этот файл в .env и заполнить реальными значениями.
# .env НЕ должен попадать в git (см. .gitignore).

# ── LLM-провайдер ─────────────────────────────────────────────────────────────
# "lmstudio" — локально через LM Studio (рекомендуется)
# "groq"     — облако Groq (запасной)
LLM_PROVIDER=lmstudio

# LM Studio: сервер запускается кнопкой "Start Server" в LM Studio
# URL по умолчанию localhost:1234 — менять не нужно
LLM_BASE_URL=http://localhost:1234/v1
LLM_API_KEY=lm-studio
# "auto" — Ника сама спросит LM Studio, какая модель сейчас загружена
# (удобно при переключении моделей в LM Studio без правки .env).
# Либо указать конкретное имя модели, как в LM Studio.
LLM_MODEL=auto

# Groq (если LLM_PROVIDER=groq):
# LLM_BASE_URL=https://api.groq.com/openai/v1
# LLM_API_KEY=gsk_...
# LLM_MODEL=openai/gpt-oss-120b

# ── TTS-провайдер ─────────────────────────────────────────────────────────────
# "xtts"       — локально, EN+RU, клонирование голоса (рекомендуется)
# "piper"      — локально, быстро, только EN
# "elevenlabs" — облако, естественно, но платно
TTS_PROVIDER=xtts

# XTTS-v2 — голос для клонирования (путь к wav-файлу, 5-30 сек чистой речи)
# Оставить пустым → будет использоваться стандартный голос модели
XTTS_SPEAKER_WAV=
# "auto" = язык определяется автоматически по тексту, либо "en" или "ru"
XTTS_LANGUAGE=auto

# Согласие с некоммерческой лицензией Coqui (CPML) для автозагрузки модели XTTS-v2
# без интерактивного вопроса. Полный текст: https://coqui.ai/cpml
# Ставь =1 только если используешь Нику некоммерчески (личный проект).
COQUI_TOS_AGREED=1

# Piper TTS (если TTS_PROVIDER=piper)
PIPER_VOICE_PATH=

# ElevenLabs (если TTS_PROVIDER=elevenlabs)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_MODEL=eleven_multilingual_v2

# ── Мост Python <-> Godot ─────────────────────────────────────────────────────
BRIDGE_HOST=localhost
BRIDGE_PORT=8765

# ── Whisper ───────────────────────────────────────────────────────────────────
WHISPER_MODEL=base

# ── Веб-поиск (погода / курсы валют / новости / поиск) ────────────────────────
# Город по умолчанию для погоды, если пользователь не назвал город явно.
WEATHER_CITY=Moscow
# Валютная пара по умолчанию для "what's the exchange rate" без уточнения.
DEFAULT_FX_BASE=USD
DEFAULT_FX_TARGET=RUB
