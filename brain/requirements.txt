# ── Ядро ──────────────────────────────────────────────────────────────────────
requests
sounddevice
numpy
openai-whisper
python-dotenv
websockets

# ── TTS ───────────────────────────────────────────────────────────────────────
# XTTS-v2 (локальный, EN+RU, клонирование голоса) — РЕКОМЕНДУЕТСЯ
# Размер модели ~2 GB, скачивается автоматически при первом запуске
TTS

# Piper (запасной, только EN, быстрее) — раскомментировать если нужен
# piper-tts

# ElevenLabs — не нужен отдельный пакет, работает через requests напрямую

# ── Управление ПК ─────────────────────────────────────────────────────────────
pyautogui
pyperclip
