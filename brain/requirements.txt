# ── Ядро ──────────────────────────────────────────────────────────────────────
requests
sounddevice
numpy
openai-whisper
python-dotenv
websockets

# ── TTS ───────────────────────────────────────────────────────────────────────
# Piper (сейчас используется: локально, только EN, быстро) — TTS_PROVIDER=piper
piper-tts

# XTTS-v2 (локальный, EN+RU, клонирование голоса, медленнее на CPU) — запасной
# ВАЖНО: пакет "TTS" с PyPI НЕ ставится на Python 3.12 — нужен форк coqui-tts.
# Также нужен системный FFmpeg (с DLL) для клонирования голоса через speaker_wav.
# coqui-tts
# TTS_PROVIDER=xtts в .env

# ElevenLabs — не нужен отдельный пакет, работает через requests напрямую

# ── Управление ПК ─────────────────────────────────────────────────────────────
pyautogui
pyperclip

# ── Веб-поиск (погода / курсы валют / новости / поиск) ────────────────────────
ddgs
